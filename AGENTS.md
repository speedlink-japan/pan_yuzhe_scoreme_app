# プロジェクトルール

## Meタブの着せ替え・キャラクター要素

- Meタブに着せ替え、アクセサリー、家具、背景などの要素を追加・変更するたびに、タブのサイズ変更後もキャラクターと背景のサイズ・位置・比率が一貫しているか確認する。
- 確認時は、通常サイズだけでなく、タブを縮小・拡大した状態でもキャラクター、背景、家具の相対位置が崩れないことを確認する。
- キャラクターと背景は共有するシーン座標・基準点を使い、個別のCSSサイズ調整だけで対応しない。
- 実装後は、ブラウザでMeタブを表示し、リサイズまたは複数の表示幅で目視確認する。必要に応じてビルドと関連テストも実行する。

## Issue運用

- Issueを作成するときは、必ず現在のユーザーをassigneeに設定する。ユーザー名が不明な場合は、作成前にリポジトリまたは接続済みGitHub情報から確認する。
- Issueには必ず優先度、Label、Typeを設定する。未設定のまま作成しない。
- Issueの本文には、目的、受け入れ条件、確認方法を記載する。

## 実装開始前の手順

- 実装・コード変更を開始する前に、対応するIssueを作成する。
- Issue番号と受け入れ条件を確認してから、ブランチ作成・コード変更・実装に進む。
- 既存Issueで対応する場合は、新規作成の代わりに対象Issueを明示してから実装する。
